import { createSlice } from '@reduxjs/toolkit';

const itemSlice = createSlice({
    name: 'item',
    initialState: {
    },
    reducers: {
    },
    extraReducers: {
    },
});

export const {
} = itemSlice.actions;

export default itemSlice.reducer;